package-as: TotemHud

# assets ships the in-game .tga icon and the monospace font with its
# license; the png master is project art and stays repo-only
ignore:
  - assets/logo.png
  - README.md
  - .gitattributes
  - etc
